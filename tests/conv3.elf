% Conversion test for eta-expansion (well, not really)
A : type.
a : A -> A.

P : (A -> A) -> type.
Q : P [x : A] a x -> type.

p : {f : A -> A} P [x : A] f x.

c : Q (p [x : A] a x).
d : Q (p [x : A] a x).
