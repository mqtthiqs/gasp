% test dependencies among arguments with same types

A : type.
P : A -> type.
B : type.
Q : B -> type.

f : {x : A} {p : P x} B.
g : {x : A} P x.
a : A.
b : B.

h : Q (f a (g a)).
j : P (([x : A] x) a).
