(nat : Type).
(o : nat).
(s : nat -> nat).
(1 = s o).

(list : Type -> Type).
(nil : (A:Type). list A).
(cons : (A:Type). A -> list A -> list A).

(ln = list nat).
(nn = nil nat).
(1n = cons nat 1 nn).

(commit : list nat -> Type).

commit 1n
