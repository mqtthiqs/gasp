(nat : Type).
(o : nat).
(s : nat -> nat).
(1 = s o : nat).

(list : Type -> Type).
(nil : (A:Type). list A).
(cons : (A:Type). A -> list A -> list A).

(ln = list nat : Type).
(nn = nil nat : list nat).
(1n = cons nat 1 nn : list nat).

(commit : list nat -> Type).

commit 1n
